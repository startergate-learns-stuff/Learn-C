#include <stdio.h>

/*void main() {
	int gen, exc;
	double avg;
	printf("컴퓨터 일반 점수 : ");
	scanf("%d", &gen);
	printf("엑셀 점수 : ");
	scanf("%d", &exc);
	avg = (gen + exc) / 2.0;
	if (gen >= 40 && exc >= 40 && avg >= 60)
	{
		printf("합격\n");
	}
	else
	{
		printf("불합격\n");
	}
}*/
//시험

/*void main() {
	int a;
	scanf("%d", &a);
	if (a > 0)
	{
		printf("%d는 양수입니다.", a);
	}
	else if (a < 0)
	{
		printf("%d는 음수입니다.", a);
	}
	else
	{
		printf("%d입니다.", a);
	}
}*/