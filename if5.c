#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*void main() {
  int k, a;
  printf("가위(0), 바위(1) 보(2): ");
  scanf("%d", &a);
  srand(time(0));
  k = rand() % 3;
  printf("사용자: ");
  switch (k)
  {
  case 0:
	  printf("가위");
	  break;
  case 1:
	  printf("바위");
	  break;
  case 2:
	  printf("보");
	  break;
  }
  printf(" -> ");
  if (k == a)
  {
	  printf("비겼습니다!\n");
  }
  else if (a - k == 1 || a - k == -2)
  {
	  printf("이겼습니다!\n");
  }
  else
  {
	  printf("졌습니다!\n");
  }
}*/
